
{% extends 'FrontBundle:Default:index.html.twig'%}

{%block ubicacion%}
<a href="{{path('categoria')}}">Categoria</a> <div class="breadcrumb_divider"></div><!--<a class="current">Editar</a>-->
{%endblock ubicacion%}

{%block cuerpo1%}

<article class="module width_3_quarter">
		<header><h3 class="tabs_involved">Lista Categoria</h3>
		<ul class="tabs">
   			<li><a href="#tab1">Pagina 1</a></li>    		
		</ul>
		</header>

		<div class="tab_container">
			<div id="tab1" class="tab_content">
			<table class="tablesorter" cellspacing="0"> 
			<thead> 
                            <tr>
                                <!--<th>Id</th>-->
                                <th>Nombre</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% for entity in entities %}
                            <tr>
                                {#<td><a href="{{ path('categoria_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>#}
                                <td>{{ entity.Nombre }}</td>
                                <td>                                    
                                    <a href="{{ path('categoria_show', { 'id': entity.id }) }}">Detalles</a>
                                        
                                    <a href="{{ path('categoria_edit', { 'id': entity.id }) }}">Editar</a>
                                        
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>

                    <form action="{{ path('categoria_new')}}">                           
                            <input type="image" src="{{asset('bundles/front/images/icn_new_article.png')}}">                                   
                                                          
                    <form> 
                        
                        </div>
                    </div>
                        
                    <footer>
                        <div class="submit_link">
                        </div>
                    </footer>
                                    
</article><!-- end of content manager article -->

{%endblock cuerpo1%}

