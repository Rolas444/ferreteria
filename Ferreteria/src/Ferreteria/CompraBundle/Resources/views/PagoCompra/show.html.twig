<h1>PagoCompra</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Numeropago</th>
            <td>{{ entity.NumeroPago }}</td>
        </tr>
        <tr>
            <th>Monto</th>
            <td>{{ entity.Monto }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('pagocompra') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('pagocompra_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('pagocompra_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
